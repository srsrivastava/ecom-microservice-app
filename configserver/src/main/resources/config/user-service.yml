server:
  port: 8082

spring:
  data:
    mongodb:
      uri: ${URI}
      database: ${DB}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true